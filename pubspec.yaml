name: tyre_ms
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  firebase_core: ^3.4.0
  # Library Kamera & Folder (Minggu 3)
  image_picker: ^1.0.7
  path_provider: ^2.1.2
  shared_preferences: ^2.2.3
  cloud_firestore: ^5.4.0
  firebase_auth: ^5.2.0
  url_launcher: ^6.3.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/logo_tms.png